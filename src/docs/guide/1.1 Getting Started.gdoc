h4. The first step is installing the plugin. Add a plugin dependency in BuildConfig.groovy:

{code}
plugins {
   runtime ':db-reverse-engineer:0.5'
}
{code}

There are no required initialization steps but there are some configuration options described in the [next section|guide:2 Configuration].
